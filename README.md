# relay

Durable LLM-agent workflow engine. Temporal for durable execution,
LangGraph for agent orchestration, Pydantic-validated LLM outputs.

Work in progress.

```sh
pip install -e ".[dev]"
pytest
```

MIT licensed.
